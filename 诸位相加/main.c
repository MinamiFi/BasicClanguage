#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x,y;
    scanf("%d",&x);
    while(1)
    {
        y=x/1000;
        if(y!=0)
        {
            printf("%d+",y)
        }
    }
    return 0;
}
